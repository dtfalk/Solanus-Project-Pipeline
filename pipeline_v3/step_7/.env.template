# step_7 model API keys — copy to step_7/.env (gitignored) and fill in. Never commit .env.
# Only the providers you actually use need keys.
GEMINI_API_KEY=          

# OpenAI embeddings (text-embedding-3-*)
OPENAI_API_KEY=          

# Voyage embeddings + reranker (Anthropic-recommended)
VOYAGE_API_KEY=

# optional: Cohere reranker
COHERE_API_KEY=

# only if calling Claude directly (otherwise via GCP Model Garden)
# ANTHROPIC_API_KEY=     
