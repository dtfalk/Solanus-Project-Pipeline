# Azure Document Intelligence — prebuilt-read (OCR only; layout comes from our labels).
# Get both from: Azure portal -> your DI resource -> "Keys and Endpoint".
# Copy this file to .env in this same folder and fill in your values:
#   cp .env.template .env
AZURE_DI_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/
AZURE_DI_KEY=<your-key>
